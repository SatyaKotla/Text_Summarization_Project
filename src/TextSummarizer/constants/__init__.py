#creating constants that to be used throughout the code
#we are creating constants for the file paths config.yaml and params.yaml

from pathlib import Path

CONFIG_FILE_PATH = Path('config/config.yaml')
PARAMS_FILE_PATH = Path('params.yaml')